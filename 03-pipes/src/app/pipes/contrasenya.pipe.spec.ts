import { ContrasenyaPipe } from './contrasenya.pipe';

describe('ContrasenyaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrasenyaPipe();
    expect(pipe).toBeTruthy();
  });
});
